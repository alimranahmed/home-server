services:
    vaultwarden:
        image: vaultwarden/server:latest
        container_name: vaultwarden
        restart: unless-stopped
        volumes:
            - /home-server-data/vaultwarden/data/:/data/
        ports:
            - 127.0.0.1:8000:80 # only works with localhost if not https